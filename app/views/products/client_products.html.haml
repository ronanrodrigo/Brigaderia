#fundo
  #conteudo
    .title-section
      Nossas delícias
    .text
      - @products.each do |product|
        %a.delicious_item
          %p
            = product.name
            = product.description
            = product.price
          = image_tag product.image.url(:thumb)
