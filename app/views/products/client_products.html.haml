#fundo
  #conteudo
    .title-section
      Nossas delícias
    .some_products.all
      %ul.product_index_list
        - @products.each_with_index do |product, index|
          %li.product_left.all_products
            .product_img_left
              = image_tag product.image.url(:short)
            %b.product_name
              = product.name
            %br
            = product.description